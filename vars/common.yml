user: "{{ lookup('env','USER') }}"
home: "{{ lookup('env','HOME') }}"
computer_name: "{{ lookup('pipe', 'scutil --get ComputerName') }}"
is_home: "{{ computer_name == 'Gaspar’s Mac mini' or computer_name == 'Gaspar’s MacBook Pro' }}"
is_work: "{{ is_home|bool == false }}"
